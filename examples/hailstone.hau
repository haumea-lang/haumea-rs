/* Calculates the hailstone sequence for a given number */

to collatz with (n) do
  while n != 1 do
    display(n)
    if n modulo 2 = 0 then
      set n to n / 2
    else
      set n to 3 * n + 1
  end
  display(1)
end

to main do
  collatz(read())
end
